---
paths:
    - 'app/Models/**'
---

# Models

## Use PHP attribute syntax for #[Fillable] and #[Hidden]

Declare fillable and hidden fields using the PHP attribute syntax `#[Fillable([...])]` and `#[Hidden([...])]`. Do not use the `$fillable` or `$hidden` properties.
